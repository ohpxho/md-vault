# DNS Basics

- What does DNS stand for?
- How can DNS be compared to a phonebook?
- What is the primary purpose of DNS?
- Why is DNS important for users when accessing servers?

# Types of DNS Servers

- What are the four main types of DNS servers?
- What is the roles of sa recursive resolver?
- What does a root nameserver do?
- What is the function of a TLD nameserver?
- What information does an authorative nameserver provide?
- Why do these four types of DNS servers need to work in harmony?

# DNS Process

- What is the first step when a client queries a domain name?
- What does a DNS recursive resolver do when it receives a query?
- What happens if the recursive resolver has the requested data in its cache?
- What is the role of the root nameserver in the DNS query process?
- How does a TLD server respond to a resolver's query
- How many root server clusters exist worldwide, and why are they distributed?
- What does the authorative server return to the resolver?
- What happens if the authorative server returns a CNAME records?
- Why does the recursive resolver cache results?
- What information does an authorative nameserver provides?

# Details about Each Server

- Where are DNS recursive resolvers usually hosted?
- Name two examples of public DNS providers.
- How many root server clusters exist worldwide?
- What is the difference between generic TLDs servers like .com?
- What is the final step in a DNS query process?
