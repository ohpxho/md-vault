# C++ Notes

- What would happen if you return non-zero value in C++? And, what if nothing is return?
- What is the standard library of c++?
- How will you declare or import external libraries into your c++ program?
- What version of C++ does `import` is available?
- How can you make names from defined/imported libraries/programs visible through out the program? E.g `std::cout` => `cout`
- What is the purpose of `templates`
- Why and when do you use a template?
- What is the purpose of `auto`?
- Why upon declaration using `auto`, you need to initialize the value of the variables and not leave them uninitialize?
- To declare a function, you need these 3, what are they?
- What is the importance of parameter names in function declarations? Why does compiler igore them and only cares about the type?
- What are the 4 definition that the compiler need to identify the identity of a function?
- Why does return type is not included in the identity of the function?
