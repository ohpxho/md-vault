# Enums

- What is an enum?
- How does an enum work?
- How does an enum is stored in the memory? And, where is it stored?
- When to use an enum?
- What is the default value of an enum?
- How will you assign a value to an enum manually? Is it even possible to assign values manually?
- What are the different approach to implement an enum? And, what are the differences among, and why is this better than another?
