# Terms

## Fan-out Architecture

- What is fan-out architecture, and how does it handle multiple outputs in parallel?
- How does fan-out architecture affect the rate at which a system can process and deliver multiple outputs?

## Fan-in Architecture

- What is fan-in architecture, and how does it handle multiple inputs in parallel?
- How does fan-in architecture affect the rate at which a system can process and accept multiple inputs?

## Network Latency

- What is network latency, and what does network latency measure?
- How does high latency affect network performance compared to low latency?
